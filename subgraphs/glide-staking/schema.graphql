# Manual Glide Staking 
type ManualGlideStaking @entity {
  id: ID!

  # stake amount
  stakeAmount: BigInt!
}

# Manual Glide Staking 
type AutoGlideStaking @entity {
  id: ID!

  # share amount
  shareAmount: BigInt!
  
  # stake amount
  stakeAmount: BigInt!
}